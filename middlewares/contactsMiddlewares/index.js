const { checkContactById } = require("./checkContactById");
const { checkContactInput } = require("./checkContactInput");
const { checkFavoriteInput } = require("./checkFavoriteInput");

module.exports = {
  checkContactById,
  checkContactInput,
  checkFavoriteInput,
};
